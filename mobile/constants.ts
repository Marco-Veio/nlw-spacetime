import Constants from "expo-constants";

export const GITHUB_CLIENT_ID = Constants.expoConfig?.extra?.GITHUB_CLIENT_ID;
export const API_URL = Constants.expoConfig?.extra?.API_URL;
export const SCHEME = Constants.expoConfig?.extra?.SCHEME;
